"""Dependency-light package for the removable Docling text experiment.

Keep this module intentionally empty.  The isolated parser environment runs
``python -m experiments.docling_text_pipeline.extract_worker`` and should not
need to import the production report pipeline or its dependencies.
"""
